fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlmodel==0.0.22
jinja2==3.1.6
python-multipart==0.0.20
weasyprint==63.1
python-dotenv==1.1.0
# Schritt 3 — central configuration. pydantic (v2) is already present
# transitively via fastapi/sqlmodel; pydantic-settings is the separate
# BaseSettings package. Loose pin (repo style for non-core deps) to stay
# compatible with whatever pydantic 2.x fastapi/sqlmodel resolve.
pydantic-settings>=2.4,<3
bcrypt==5.0.0
itsdangerous==2.2.0
anthropic>=0.40.0
markdown==3.8
# Pinned: mcp 1.20+ requires starlette 1.0+, which is incompatible with fastapi 0.115.
# Bump together with fastapi when upgrading.
mcp==1.19.0
# E-Rechnung / ZUGFeRD 2.x / Factur-X
drafthorse>=2024.4
pikepdf>=9.0
# VIES (USt-IdNr.) Validierung
zeep>=4.3
# Schritt 9 — DB migrations. Two independently versioned trees (CRM +
# Billing, separate version tables) replace the implicit create_all schema
# evolution. Loose pin (repo style for non-core deps); 1.13+ targets the
# SQLAlchemy 2.x that sqlmodel pulls.
alembic>=1.13,<2
