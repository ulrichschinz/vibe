"""interfaces.api — REST router for agents (placeholder).

Soll: today's `routes/api.py` (X-API-Key, validate_api_key) with the
RFC-7807 error coercion centralized into one `app.core.errors` mapper
instead of inline per endpoint (Schritt 8). Empty by design in Schritt 2.
"""
