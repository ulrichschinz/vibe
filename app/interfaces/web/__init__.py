"""interfaces.web — Jinja2 UI routers + templates (placeholder).

Soll: the thin web handlers (today the form-based mutation routes in
`routes/leads.py`, `routes/invoices.py`, `routes/proposals.py`,
`routes/ai.py`, `routes/admin.py`, `routes/auth.py`) plus `templates/`,
auto-bound from `app.domains.*`. Empty by design in Schritt 2.
"""
