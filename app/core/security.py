"""app.core.security — auth primitives (placeholder until Schritt 6).

Soll: password hashing and the `require_login/_editor/_admin` FastAPI
dependencies move here verbatim from the (already clean) `services/auth.py`
when business logic leaves the routes. Empty by design in Schritt 2.
"""
